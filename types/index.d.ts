// types/index.d.ts

// Example: global types for your project
export interface Dealer {
  id: string;
  name: string;
  email: string;
  created_at: string;
}

export interface AssessmentAnswer {
  question: string;
  answer: string;
}

// Add more types/interfaces as needed
